/* Alpine-compatible micro runtime for PortD's small x-data and x-bind needs. */
document.addEventListener('submit', function (event) { var root = event.target.closest('[x-data]'); if (root) { var button = root.querySelector('[x-bind\\:disabled], [\\:disabled]'); if (button) button.disabled = true; } });
